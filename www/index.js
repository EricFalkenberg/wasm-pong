import * as game from "rust-wasm";
game.run();

// function gameLoop() {
//   rust.paint_screen(universe);
//   universe = rust.tick(universe);
//   setTimeout(gameLoop, 1000);
// }
//
// gameLoop();
